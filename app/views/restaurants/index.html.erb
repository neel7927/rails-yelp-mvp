<h1> List of Restaurants </h1>

<ul>
<% @restaurants.each do |restaurant| %>
  <li><%= restaurant.name %> <%= link_to "See details", restaurant_path(restaurant), class: "btn btn-primary"%> </li>
<% end %>
</ul>
